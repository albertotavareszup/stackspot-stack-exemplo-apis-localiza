type: app
description: Default stackfile usecase description
# template used to create the project
template: apis-localiza/<template-name>
inputs:
  some_input: value
# list of plugins to be applied after project creation
plugins:
  - plugin: apis-localiza/<plugin-name>
    inputs:
      some_input: value
      some_list:
        - item1
        - item2
